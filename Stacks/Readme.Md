# Stack Comparison Project

This Java program implements two stacks using linked lists, fills them with random integers, and compares the top values of each stack to track how often one exceeds the other.
